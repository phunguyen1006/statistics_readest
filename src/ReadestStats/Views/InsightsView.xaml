<UserControl x:Class="ReadestStats.Views.InsightsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:charts="clr-namespace:ReadestStats.Controls" AutomationProperties.Name="Insights page">
  <UserControl.Resources>
    <Style x:Key="InsightCardItem" TargetType="ListBoxItem">
      <Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="VerticalContentAlignment" Value="Stretch"/><Setter Property="Margin" Value="6"/><Setter Property="Padding" Value="16"/><Setter Property="MinHeight" Value="132"/><Setter Property="Cursor" Value="Hand"/><Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem"><Border x:Name="Body" Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="10" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Body" Property="Background" Value="{DynamicResource SurfaceHover}"/><Setter TargetName="Body" Property="BorderBrush" Value="{DynamicResource BorderStrong}"/></Trigger><Trigger Property="IsSelected" Value="True"><Setter TargetName="Body" Property="Background" Value="{DynamicResource SurfaceActive}"/><Setter TargetName="Body" Property="BorderBrush" Value="{DynamicResource Focus}"/><Setter TargetName="Body" Property="BorderThickness" Value="2"/></Trigger><Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Body" Property="BorderBrush" Value="{DynamicResource Focus}"/><Setter TargetName="Body" Property="BorderThickness" Value="2"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
    </Style>
  </UserControl.Resources>
  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <StackPanel Margin="26,22,26,28" MaxWidth="1480">
      <UniformGrid Columns="4" Margin="0,0,0,16">
        <charts:StatCard Label="CONSISTENCY" Value="{Binding Period.ConsistencyPercent,StringFormat={}{0:0}%}" Detail="{Binding ConsistencyDetail}" Command="{Binding NavigateCommand}" CommandParameter="Activity" Help="Open the activity calendar behind this consistency figure."/>
        <charts:StatCard Label="TYPICAL SESSION" Value="{Binding SessionProfile.Median,Converter={StaticResource Duration}}" Detail="Median active reading" Command="{Binding NavigateCommand}" CommandParameter="Sessions" Help="Open the sessions used to calculate this median."/>
        <charts:StatCard Label="COMMON WINDOW" Value="{Binding CommonWindowLabel}" Detail="{Binding CommonWindow.Share,StringFormat={}{0:P0} of reading}" Command="{Binding OpenInsightCommand}" CommandParameter="{Binding AllInsights[0]}" Help="Show how the strongest reading window was calculated."/>
        <charts:StatCard Label="AVG / CALENDAR DAY" Value="{Binding Period.AveragePerCalendarDay,Converter={StaticResource Duration}}" Detail="Eligible days only" Command="{Binding NavigateCommand}" CommandParameter="Activity" Help="Open the activity data used for this average."/>
      </UniformGrid>

      <Grid Margin="0,0,0,16">
        <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
        <Border Style="{StaticResource ChartCard}" Margin="0,0,8,0">
          <StackPanel>
            <TextBlock Text="Session profile" Style="{StaticResource SectionTitle}"/>
            <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="SHORTEST" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding SessionProfile.Shortest,Converter={StaticResource Duration}}"/></StackPanel><StackPanel Grid.Column="1"><TextBlock Text="AVERAGE" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding SessionProfile.Average,Converter={StaticResource Duration}}"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Text="LONGEST" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding SessionProfile.Longest,Converter={StaticResource Duration}}"/></StackPanel></Grid>
            <charts:BarChart ItemsSource="{Binding SessionHistogram}" Height="175" MaxBarWidth="30" Margin="0,10,0,0"/>
            <TextBlock Style="{StaticResource Muted}"><Run Text="Middle half: "/><Run Text="{Binding SessionProfile.P25,Converter={StaticResource Duration}}"/><Run Text=" – "/><Run Text="{Binding SessionProfile.P75,Converter={StaticResource Duration}}"/></TextBlock>
          </StackPanel>
        </Border>
        <Border Grid.Column="1" Style="{StaticResource Card}" Margin="8,0,0,0">
          <StackPanel><TextBlock Text="Weekday vs weekend" Style="{StaticResource SectionTitle}"/><UniformGrid Columns="2"><StackPanel><TextBlock Text="WEEKDAY AVG" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding WeekdayWeekend.AverageWeekdaySeconds,Converter={StaticResource Duration}}" FontSize="22" FontWeight="SemiBold"/></StackPanel><StackPanel><TextBlock Text="WEEKEND AVG" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding WeekdayWeekend.AverageWeekendSeconds,Converter={StaticResource Duration}}" FontSize="22" FontWeight="SemiBold"/></StackPanel></UniformGrid><TextBlock Text="{Binding WeekdayWeekend.Summary}" Style="{StaticResource Muted}" Margin="0,14,0,0"/></StackPanel>
        </Border>
      </Grid>

      <DockPanel Margin="0,0,0,8"><TextBlock Text="Click any card to see the calculation" DockPanel.Dock="Right" Style="{StaticResource Muted}" VerticalAlignment="Center"/><TextBlock Text="Derived insights" Style="{StaticResource SectionTitle}" Margin="0"/></DockPanel>
      <ListBox ItemsSource="{Binding AllInsights}" SelectedItem="{Binding SelectedInsight}" ItemContainerStyle="{StaticResource InsightCardItem}" Background="Transparent" BorderThickness="0" Padding="0" HorizontalContentAlignment="Stretch" AutomationProperties.Name="Derived insights">
        <ListBox.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="3"/></ItemsPanelTemplate></ListBox.ItemsPanel>
        <ListBox.ItemTemplate>
          <DataTemplate>
              <Grid AutomationProperties.Name="{Binding Title}"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <DockPanel><TextBlock Text="{Binding SampleStatus}" DockPanel.Dock="Right" Style="{StaticResource Muted}"/><TextBlock Text="{Binding Category}" Style="{StaticResource Eyebrow}"/></DockPanel>
                <TextBlock Grid.Row="1" Text="{Binding Title}" FontSize="17" FontWeight="SemiBold" Margin="0,8,0,4" TextWrapping="Wrap"/>
                <TextBlock Grid.Row="2" Text="{Binding Description}" Style="{StaticResource Muted}"/>
                <TextBlock Grid.Row="3" Text="Why this insight?  →" Foreground="{DynamicResource TextSecondary}" FontSize="11" FontWeight="SemiBold" Margin="0,10,0,0"/>
                <Border Grid.Row="4" Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="7" Padding="10" Margin="0,10,0,0">
                  <Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding IsSelected,RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></Border.Style>
                  <StackPanel><TextBlock Text="CALCULATION" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding Evidence}" Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap" Margin="0,6,0,0"/></StackPanel>
                </Border>
              </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </StackPanel>
  </ScrollViewer>
</UserControl>

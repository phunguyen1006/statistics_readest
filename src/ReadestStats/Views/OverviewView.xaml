<UserControl x:Class="ReadestStats.Views.OverviewView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:loc="clr-namespace:ReadestStats.Localization" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:charts="clr-namespace:ReadestStats.Controls" AutomationProperties.Name="{loc:Loc Key=s4D9F035682530698}">
  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><Grid Margin="26,22,26,28"><StackPanel MaxWidth="1480">
    <Border Style="{StaticResource EditorialSection}" Visibility="{Binding NoPeriodData,Converter={StaticResource BoolVis}}" Margin="0,0,0,16"><StackPanel Margin="10" HorizontalAlignment="Center"><TextBlock Text="{loc:Loc Key=sF20FF99979851972}" FontSize="18" FontWeight="SemiBold"/><TextBlock Text="{loc:Loc Key=s2758D569E4415384}" Style="{StaticResource Muted}" Margin="0,6,0,0"/></StackPanel></Border>
    <Grid Margin="0,0,0,16">
      <Grid.ColumnDefinitions><ColumnDefinition Width="1.1*"/><ColumnDefinition Width="1.1*"/><ColumnDefinition Width="0.8*"/></Grid.ColumnDefinitions>
      <Border Style="{StaticResource EditorialSection}" Margin="0,0,8,0"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions><TextBlock Text="{loc:Loc Key=sA9CD8378200C194F}" Style="{StaticResource Eyebrow}"/><StackPanel Grid.Row="1" Margin="0,9"><TextBlock Text="{Binding DailyNote.BookTitle}" FontWeight="SemiBold" FontSize="16" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding DailyNote.Text}" Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap" TextTrimming="WordEllipsis" MaxHeight="42" Margin="0,5,0,0"/></StackPanel><Button Grid.Row="2" Content="{loc:Loc Key=s90877538C0D1EED4}" Command="{Binding NavigateCommand}" CommandParameter="Notes" HorizontalAlignment="Left" Padding="10,5"/></Grid></Border>
      <Border Grid.Column="1" Style="{StaticResource EditorialSection}" Margin="8,0"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions><TextBlock Text="{loc:Loc Key=s15BDE8B311363D47}" Style="{StaticResource Eyebrow}"/><StackPanel Grid.Row="1" Margin="0,9"><TextBlock Text="{Binding RandomNote.BookTitle}" FontWeight="SemiBold" FontSize="16" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding RandomNote.Text}" Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap" TextTrimming="WordEllipsis" MaxHeight="42" Margin="0,5,0,0"/></StackPanel><StackPanel Grid.Row="2" Orientation="Horizontal"><Button Content="{loc:Loc Key=sAE448AC86C4E8E4D}" Command="{Binding RandomNoteCommand}" Padding="10,5" Margin="0,0,7,0"/><Button Content="{loc:Loc Key=s2348F99874421257}" Command="{Binding NavigateCommand}" CommandParameter="Notes" Padding="10,5"/></StackPanel></Grid></Border>
      <Border Grid.Column="2" Style="{StaticResource EditorialSection}" Margin="8,0,0,0"><StackPanel><TextBlock Text="{loc:Loc Key=s87DDBB56C40D4EC2}" Style="{StaticResource Eyebrow}"/><TextBlock Text="{loc:Loc Key=s931DF59B8029BA79}" Style="{StaticResource Muted}" Margin="0,7,0,12" TextWrapping="Wrap"/><Button Content="{loc:Loc Key=sF88F7DFBEAECAA0B}" Command="{Binding NavigateCommand}" CommandParameter="Manual log" HorizontalAlignment="Stretch" Margin="0,0,0,7"/><Button Content="{loc:Loc Key=sF97EF214757E8C45}" Command="{Binding NavigateCommand}" CommandParameter="Books" HorizontalAlignment="Stretch"/></StackPanel></Border>
    </Grid>
    <UniformGrid Columns="6" Margin="0,0,0,16">
      <charts:StatCard Label="{loc:Loc Key=sA47D12EF523E1199}" Value="{Binding Period.TotalSeconds,Converter={StaticResource Duration}}" Detail="{Binding ComparisonText,Converter={StaticResource UiText}}" Command="{Binding NavigateCommand}" CommandParameter="Activity" Help="{loc:Loc Key=s58396EFA727A732A}"/>
      <charts:StatCard Label="{loc:Loc Key=s8B82BA330F5453A3}" Value="{Binding Period.ActiveDays}" Detail="{Binding Period.ConsistencyPercent,StringFormat={}{0:0}% consistency}" Command="{Binding NavigateCommand}" CommandParameter="Activity" Help="{loc:Loc Key=s14050CC229DE85F7}"/>
      <charts:StatCard Label="{loc:Loc Key=s67D31DDAE025EE86}" Value="{Binding CurrentStreakLabel,Converter={StaticResource UiText}}" Detail="{Binding LongestStreakLabel,Converter={StaticResource UiText}}" Command="{Binding NavigateCommand}" CommandParameter="Goals" Help="{loc:Loc Key=s587CC163DC040342}"/>
      <charts:StatCard Label="{loc:Loc Key=s74FDFD143A986616}" Value="{Binding Period.AveragePerActiveDay,Converter={StaticResource Duration}}" Detail="{loc:Loc Key=s4EFFEB5ACE33C23B}" Command="{Binding NavigateCommand}" CommandParameter="Statistics" Help="{loc:Loc Key=s59DADB160AA3EE47}"/>
      <charts:StatCard Label="{loc:Loc Key=s1225AE6C1AE69DCB}" Value="{Binding Period.SessionCount}" Detail="{Binding Period.AverageSessionSeconds,Converter={StaticResource Duration},StringFormat=Avg {0}}" Command="{Binding NavigateCommand}" CommandParameter="Sessions" Help="{loc:Loc Key=sA85122E69708615C}"/>
      <charts:StatCard Label="{loc:Loc Key=s173C4199F3559B96}" Value="{Binding Period.ActiveBooks}" Detail="{Binding TopBookLabel}" Command="{Binding NavigateCommand}" CommandParameter="Books" Help="{loc:Loc Key=sAA911DA3BFD23B43}"/>
    </UniformGrid>
    <TextBlock Text="{Binding StreakContinuation,Converter={StaticResource UiText}}" Foreground="{DynamicResource TextSecondary}" Margin="2,0,0,12"/>
    <Border Style="{StaticResource EditorialSection}" Margin="0,0,0,16" Visibility="{Binding HasContinueReading,Converter={StaticResource BoolVis}}"><StackPanel><DockPanel><TextBlock DockPanel.Dock="Right" Text="{loc:Loc Key=s308951F02D347143}" Style="{StaticResource Muted}"/><TextBlock Text="{loc:Loc Key=s244323BC7F6F61A1}" Style="{StaticResource Eyebrow}"/></DockPanel><TextBlock Text="{loc:Loc Key=s2990F72A471C0D96}" Style="{StaticResource Muted}" Margin="0,3,0,10"/><ItemsControl ItemsSource="{Binding ContinueReading}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="3"/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><Border BorderBrush="{DynamicResource Border}" BorderThickness="1" Padding="12" Margin="0,0,10,0"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="42"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><Border Width="34" Height="48" Background="{DynamicResource SurfaceAlt}" BorderBrush="{DynamicResource Border}" BorderThickness="1"><Image Source="{Binding CoverPath,Converter={StaticResource CoverImage},ConverterParameter=80}" Stretch="UniformToFill"/></Border><StackPanel Grid.Column="1" Margin="10,0,10,0"><DockPanel><TextBlock Text="{Binding Plan.Status,Converter={StaticResource UiText}}" DockPanel.Dock="Right" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/></DockPanel><TextBlock Text="{Binding Reason}" Style="{StaticResource Muted}" Margin="0,3,0,0"/><TextBlock Text="{Binding Plan.Summary,Converter={StaticResource UiText}}" Foreground="{DynamicResource TextSecondary}" FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/></StackPanel><StackPanel Grid.Column="2" VerticalAlignment="Center"><Button Content="{loc:Loc Key=s41FFD8B76AFC92A6}" Command="{Binding DataContext.OpenFocusBookCommand,RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Padding="9,4"/><Button Content="{loc:Loc Key=sCCED28C6DC3F99C2}" Command="{Binding DataContext.StartFocusSessionCommand,RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Padding="9,4" Margin="0,5,0,0"/></StackPanel></Grid></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel></Border>
    <Grid Margin="0,0,0,16"><Grid.ColumnDefinitions><ColumnDefinition Width="2.15*"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions>
      <Border Style="{StaticResource EditorialSection}" Margin="0,0,8,0"><StackPanel><Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{loc:Loc Key=s65F0FE485BF12CAD}" Style="{StaticResource SectionTitle}"/><TextBlock Text="{Binding TrendSummary,Converter={StaticResource UiText}}" Style="{StaticResource Muted}" Margin="0,-8,0,0"/><TextBlock Text="{Binding ComparisonLegend,Converter={StaticResource UiText}}" Style="{StaticResource Muted}" Margin="0,3,0,0"/></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal"><ComboBox ItemTemplate="{StaticResource LocalizedOption}" ItemsSource="{Binding TrendMetrics}" SelectedItem="{Binding TrendMetric}" Width="128" Margin="0,0,8,0" AutomationProperties.Name="{loc:Loc Key=s368B7E35FB91D370}"/><ComboBox ItemTemplate="{StaticResource LocalizedOption}" ItemsSource="{Binding Granularities}" SelectedItem="{Binding TrendGranularity}" Width="88" Margin="0,0,8,0" AutomationProperties.Name="{loc:Loc Key=sCDCA1831E69D6B5B}"/><ComboBox ItemTemplate="{StaticResource LocalizedOption}" ItemsSource="{Binding CompareModes}" SelectedItem="{Binding CompareMode}" Width="150" AutomationProperties.Name="{loc:Loc Key=sCB94AA26E7DF2CF9}"/></StackPanel></Grid><StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Visibility="{Binding IsCustomComparison,Converter={StaticResource BoolVis}}" Margin="0,8,0,0"><DatePicker SelectedDate="{Binding CompareCustomStart}" Width="130"/><TextBlock Text="{loc:Loc Key=s663EA1BFFFE5038F}" VerticalAlignment="Center" Margin="7,0"/><DatePicker SelectedDate="{Binding CompareCustomEnd}" Width="130"/></StackPanel><charts:LineChart ItemsSource="{Binding Trend}" ComparisonItemsSource="{Binding ComparisonData}" Height="206" Margin="0,12,0,0" AutomationProperties.Name="{loc:Loc Key=s136C13E89FB1957C}" AutomationProperties.HelpText="{loc:Loc Key=sF578EF86A912AC91}"/><Expander Header="{loc:Loc Key=sED630DE76C8FC978}" Margin="0,4,0,0"><Grid Margin="0,7,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><ItemsControl ItemsSource="{Binding Trend}"><ItemsControl.ItemTemplate><DataTemplate><DockPanel Margin="0,3"><TextBlock Text="{Binding Detail,Converter={StaticResource UiText}}" DockPanel.Dock="Right" Style="{StaticResource Muted}"/><TextBlock Text="{Binding Label}"/></DockPanel></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><ItemsControl Grid.Column="1" ItemsSource="{Binding ComparisonData}" Margin="18,0,0,0"><ItemsControl.ItemTemplate><DataTemplate><DockPanel Margin="0,3"><TextBlock Text="{Binding Detail,Converter={StaticResource UiText}}" DockPanel.Dock="Right" Style="{StaticResource Muted}"/><TextBlock Text="{Binding Label}"/></DockPanel></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></Grid></Expander></StackPanel></Border>
      <StackPanel Grid.Column="1" Margin="8,0,0,0"><Border Style="{StaticResource EditorialSection}" Margin="0,0,0,12"><StackPanel><TextBlock Text="{loc:Loc Key=sB1F9DA6E40BE62E4}" Style="{StaticResource Eyebrow}"/><Grid Margin="0,12,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{loc:Loc Key=sDCEFBDD318F89EFC}" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding Comparison.CurrentValue,Converter={StaticResource Duration}}" FontSize="20" FontWeight="SemiBold"/></StackPanel><StackPanel Grid.Column="1"><TextBlock Text="{Binding ComparisonPeriodLabel}" TextWrapping="Wrap" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding Comparison.PreviousValue,Converter={StaticResource Duration}}" FontSize="20" FontWeight="SemiBold"/></StackPanel></Grid><Separator Background="{DynamicResource Border}" Margin="0,12"/><TextBlock Text="{Binding ComparisonText,Converter={StaticResource UiText}}"/><TextBlock Text="{Binding AbsoluteDeltaText}" Style="{StaticResource Muted}" Margin="0,3,0,0"/></StackPanel></Border><Border Style="{StaticResource EditorialSection}"><StackPanel><TextBlock Text="{loc:Loc Key=sD6C838BAD6715933}" Style="{StaticResource Eyebrow}"/><ItemsControl ItemsSource="{Binding QuickInsights}"><ItemsControl.ItemTemplate><DataTemplate><Button Command="{Binding DataContext.OpenInsightCommand,RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource TextLinkButton}" HorizontalContentAlignment="Stretch" Padding="8" Margin="-8,6,-8,0" AutomationProperties.Name="{Binding Title}"><StackPanel><TextBlock Text="{Binding Title}" FontWeight="SemiBold"/><TextBlock Text="{Binding Description}" Style="{StaticResource Muted}" Margin="0,2,0,0"/></StackPanel></Button></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel></Border></StackPanel>
    </Grid>
    <Grid Margin="0,0,0,16"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
      <Border Style="{StaticResource EditorialSection}" Margin="0,0,8,0"><StackPanel><TextBlock Text="{loc:Loc Key=s00950F76482C8C55}" Style="{StaticResource Eyebrow}"/><TextBlock Text="{loc:Loc Key=s093272236D04570C}" Style="{StaticResource Muted}" Margin="0,3,0,5"/><charts:SegmentedBandChart ItemsSource="{Binding SourceComposition}" Height="76" AutomationProperties.Name="{loc:Loc Key=s585A11CFB133A8AF}"/><TextBlock Text="{Binding SourceCompositionSummary,Converter={StaticResource UiText}}" Style="{StaticResource Muted}" TextWrapping="Wrap"/></StackPanel></Border>
      <Border Grid.Column="1" Style="{StaticResource EditorialSection}" Margin="8,0,0,0" Visibility="{Binding HasPhysicalPageProgress,Converter={StaticResource BoolVis}}"><StackPanel><TextBlock Text="{loc:Loc Key=s2C75ADFBFEED731C}" Style="{StaticResource Eyebrow}"/><TextBlock Text="{loc:Loc Key=s58662A280E0F1399}" Style="{StaticResource Muted}" Margin="0,3,0,5"/><charts:LineChart ItemsSource="{Binding PhysicalPageProgress}" Height="92" AutomationProperties.Name="{loc:Loc Key=s3666E48356F12099}"/><Expander Header="{loc:Loc Key=s6D2A97F4F2824A8A}"><ItemsControl ItemsSource="{Binding PhysicalPageProgress}"><ItemsControl.ItemTemplate><DataTemplate><DockPanel Margin="0,2"><TextBlock Text="{Binding Detail,Converter={StaticResource UiText}}" DockPanel.Dock="Right" Style="{StaticResource Muted}"/><TextBlock Text="{Binding Label}"/></DockPanel></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></Expander></StackPanel></Border>
    </Grid>
    <Border Style="{StaticResource EditorialSection}" Margin="0,0,0,16"><StackPanel><DockPanel><TextBlock Text="{Binding Story.StreakContext,Converter={StaticResource UiText}}" DockPanel.Dock="Right" Style="{StaticResource Muted}" VerticalAlignment="Center"/><StackPanel><TextBlock Text="{loc:Loc Key=s74F81E11F5747999}" Style="{StaticResource SectionTitle}"/><TextBlock Text="{loc:Loc Key=sBEA00E9FDDF1F6B5}" Style="{StaticResource Muted}" Margin="0,-7,0,0"/></StackPanel></DockPanel><charts:FingerprintChart ItemsSource="{Binding ReadingFingerprint}" Height="82" Margin="0,8,0,0" AutomationProperties.Name="{loc:Loc Key=s9AADBA92FF7264FB}" AutomationProperties.HelpText="{loc:Loc Key=sC6CCEFB541348DC8}"/><charts:StreakStripChart ItemsSource="{Binding StreakTimeline}" Height="48" Margin="32,0,8,0" AutomationProperties.Name="{loc:Loc Key=s9B98FF8DD38DEF4E}"/></StackPanel></Border>
    <Grid Margin="0,0,0,16"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><Border Style="{StaticResource EditorialSection}" Margin="0,0,8,0"><StackPanel><DockPanel><TextBlock Text="{loc:Loc Key=sBA6B9412FF080575}" Style="{StaticResource SectionTitle}"/><ComboBox ItemTemplate="{StaticResource LocalizedOption}" DockPanel.Dock="Right" ItemsSource="{Binding PatternMetrics}" SelectedItem="{Binding PatternMetric}" Width="92" AutomationProperties.Name="{loc:Loc Key=s03EC0B311854900D}"/></DockPanel><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="210"/><ColumnDefinition/></Grid.ColumnDefinitions><charts:RadialClockChart ItemsSource="{Binding Hourly}" Height="190" Width="190" AutomationProperties.Name="{loc:Loc Key=sB7E800505820E8E7}"/><StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="{loc:Loc Key=s6F6A914EC5A3AA98}" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding PatternWindowLabel}" FontSize="20" FontWeight="SemiBold" Margin="0,4,0,3"/><TextBlock Text="{Binding PatternSummary,Converter={StaticResource UiText}}" Style="{StaticResource Muted}" TextWrapping="Wrap"/></StackPanel></Grid></StackPanel></Border><Border Grid.Column="1" Style="{StaticResource EditorialSection}" Margin="8,0,0,0"><StackPanel><TextBlock Text="{loc:Loc Key=sDB2C4C83C0C4D772}" Style="{StaticResource SectionTitle}"/><TextBlock Text="{Binding PatternWeekdaySummary,Converter={StaticResource UiText}}" Style="{StaticResource Muted}" Margin="0,-6,0,2"/><charts:BarChart ItemsSource="{Binding Weekdays}" Height="178" MaxBarWidth="24"/></StackPanel></Border></Grid>
    <TextBlock Text="{loc:Loc Key=s06C91D7209E848FA}" Style="{StaticResource SectionTitle}"/><ItemsControl ItemsSource="{Binding Records}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="4"/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><Border Style="{StaticResource EditorialSection}" Margin="0,0,12,12"><StackPanel><TextBlock Text="{Binding Name}" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding Value}" FontSize="20" FontWeight="SemiBold" Margin="0,6,0,2"/><TextBlock Text="{Binding Detail,Converter={StaticResource UiText}}" Style="{StaticResource Muted}"/></StackPanel></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
  </StackPanel></Grid></ScrollViewer>
</UserControl>
